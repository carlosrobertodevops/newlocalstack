# direnv users: `direnv allow` to auto-load envs when cd'ing into this dir.
# Adds this dir to PATH so `tflocal apply` resolves to our wrapper that
# auto-exports ARM_* + SSL_CERT_FILE before invoking the real tflocal.
PATH_add .
source ./setup-env.sh
